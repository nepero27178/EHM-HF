\def\GraphicsFolder{chapters/normal-phase/pictures}
\chapter{The normal phase}\label{chap:normal-phase}

In this chapter we apply the HF methods sketched in Sec.~\ref{sec:sketch-hf-method} to the ``normal phase''. We are studying the model in a state were no symmetry of those enumerated in Tab.~\ref{tab:symmetry-groups} is broken. Even though this phase may appear not so interesting, it actually shows a significant physical difference with respect to the conventional perfectly degenerate Fermi gas.

HF methods applied in this context will lead us to a self-consistent hopping renormalisation effect. For certain values of the non-local attraction $V$ and the doping level, a complete Mott localisation effect takes place. The resulting bands shrinking and flattening is the main net effect of the presence of $\hat H_V$ in the EHM.

This chapter is divided in three main parts. First, we discuss the symmetries of the normal phase, following the general discussion of Chap.~\ref{chap:ehm-symmetries}, and apply HF methods to derive the self-consistent approximated hamiltonian. Second, we impose some theoretical constraints on the self-consistent solution. Lastly, we present numeric results.

\section{The ``normal'' phase and its  symmetries}\label{sec:normal-phase-theory}

In the normal phase no symmetry is broken. Thus, the HF wavefunction approximating the ground state must exhibit the full $\mathrm{U}(2) \otimes \mathrm{C}_4 \otimes \mathrm{T}_1^{(x)} \otimes \mathrm{T}_1^{(y)}$ symmetry discussed in Sec.~\ref{sec:ehm-syms}. We take a slight detour from this, and instead allow for
\[
	\mathrm{C}_4 \SSB \mathrm{C}_2
\]
This is the only potential SSB we consider. It will turn out in Sec.~\ref{subsec:normal-bands-shrinking-C2} that even allowing for breaking of rotational symmetry, the ground-state actually does not break $\mathrm{C}_4$. This can be seen as a convolute way to confirm that there is not a normal-nematic phase. While this may seem as an useless complication, the derivation will set grounds for actual nematic effects found in the superconducting phase.

Our HF method relies on the application of Wick's theorem on the quartic part of the interacting hamiltonian, expressed in Eq.~\eqref{eq:hf-wick-hfc}. We will decompose the quartic operators according to Eqns.~\eqref{eq:mft-HU}, \eqref{eq:mft-HV}. Recalling Tabs.~\ref{tab:HU-sel-rules} and \ref{tab:HV-sel-rules}, for the normal phase we must consider the following contributes to the EVs of Eqns.~\eqref{eq:ehm-HU-wick}, \eqref{eq:ehm-HV(os)-wick}, \eqref{eq:ehm-HV(ss)-wick}:
\[
\begin{aligned}
	&\hat H_U 
	&&\text{Hartree contraction} \\
	&\hat H_V \text{ (o.s. sector)}
	&&\text{Hartree contraction} \\
	&\hat H_V \text{ (s.s. sector)}
	&&\text{Hartree and Fock contractions} \\
\end{aligned}	
\]
As we will see in detail and is summarised in Tab.~\ref{tab:normal-RWCs}, Hartree contractions give a chemical potential shift, while the unique Fock contraction effectively renormalises the bare bands.

\begin{table}[t]
	\centering
	\begin{tabular}{c c c l}
		\textbf{Operator} & \textbf{Sector} & \textbf{RWCs} & \textbf{Net effect} \\
		\midrule
		$\hat H_U$ && Hartree & $\mu$ shift by $-nU$ \\
		\midrule
		\multirow{3}{*}{$\hat H_V$} & o.s. & Hartree & $\mu$ shift by $nzV$ \\[0.5em]
		 & \multirow{2}{*}{s.s.} & Hartree & $\mu$ shift by $nzV$\\
		&& Fock & Bands renormalisation and resymmetrization \\
		\midrule
	\end{tabular}
	\caption{Relevant Wick's contractions and net effect in the normal phase.}
	\label{tab:normal-RWCs}
\end{table}

In the normal phase, spin is a pure physical degeneracy, being the $\uparrow$ and $\downarrow$ sectors separated; moreover, $\hat H$ is $\mathbb{Z}_2$ invariant with respect to spin inversion, $\sigma \leftrightarrow \overline{\sigma}$. This implies that all physical quantities, as is for the normal phase, should not depend on the spin index. We will perform all computations leaving $\sigma$ explicit and using this consideration at the end.

\section{Hartree effects: chemical potential renormalisation}\label{sec:normal-hartree-mu}

Let us start by the analysis of the Hartree channel. The Hartree channel is defined as operator resulting from summing Hartree-like terms originating from Wick's decomposition of the quartic parts of $\hat H$. Each quartic term decomposes as a sum of fully contracted pairs, as in Eq.~\eqref{eq:hf-wick-hfc}.

In the final hamiltonian, the net effect originating from Hartree terms will be a renormalisation of $\mu$. To derive analytically this effect is only important to the ends of computing correctly the free energy density of the approximated ground state. As is discussed in Sec.~\ref{subsec:fixed-density-role-mu}, in the context of numerical simulations at fixed density, the chemical potential is determined self-consistently at each step; thus any net effect that shifts $\mu$ is effectively ignored within (and limitedly to) the iterative algorithm.

\subsection{Hartree channel in the normal phase: analytic derivation}\label{subsec:normal-hartree-analytic}

The quartic part of $\hat H$ is given by the the local repulsion $\hat H_U$ and the non-local attraction $\hat H_V$. Applying HF methods to them, we get the decomposition of Eqns.~\eqref{eq:mft-HU}, \eqref{eq:mft-HV}. Let us deal with the Hartree sector of both separately.

\paragraph{Local repulsion.}

In the normal phase, the local repulsion $\hat H_U$ decomposes in Hartree channel as specified in Eq.~\eqref{eq:mft-HU}
\[
	\hat H_U \simeq
	U \sum_{i} \left[
		\langle 
			\hat n_{i\uparrow}
		\rangle \hat n_{i\downarrow} + \hat n_{i\uparrow} \langle 
			\hat n_{i\downarrow}
		\rangle - \langle 
			\hat n_{i\downarrow}
		\rangle \langle 
			\hat n_{i\uparrow}
			\rangle
	\right]
\]
For a translationally invariant phase such as the normal phase, it holds
\begin{equation}\label{eq:normal-density-ansatz}
	\langle \hat n_{i\sigma} \rangle = n
\end{equation}
which gives
\begin{align}
	\hat H_U &\simeq
	nU \sum_i \left[
		 \hat n_{i\uparrow} + \hat n_{i\downarrow}
	\right]
	- U \sum_i n^2 \nonumber \\
	&= nU \times \hat N - E_{\mathrm{H}/U}^{(\mathrm{N})} \label{eq:normal-HU-approx}
\end{align}
being $\hat N$ the total number operator and $ E_{\mathrm{H}/U}^{(\mathrm{N})}$ the ``contraction energy'' (also known as double counting term). In Sec.~\ref{subsec:normal-hartree-energy} we deal with it. The chemical potential is corrected by
\begin{equation}\label{eq:local-hartree-mu-shift}
	\mu \to \mu - nU
\end{equation}
The non-local attraction further corrects $\mu$.

\paragraph{Non-local attraction.}

The non-local attraction is divided in two sectors, as in Eq.~\eqref{eq:ehm-HV(ss)+HV(os)}. From Eq.~\eqref{eq:mft-HV}, its Hartree-like terms are:
\begin{multline}
	\hat H_V \simeq 
	\overbrace{
		-V \sum_{\ev{ij}} \sum_\sigma \left[
			\langle 
			\hat n_{i\sigma}
			\rangle \hat n_{j\sigma} + \hat n_{i\sigma} \langle 
			\hat n_{j\sigma}
			\rangle - \langle 
			\hat n_{i\sigma}
			\rangle \langle 
			\hat n_{j\sigma}
			\rangle
		\right]
	}^{\mathrm{s.s.}} \\
	\underbrace{
		-V \sum_{\ev{ij}} \sum_\sigma \left[
			\langle
			\hat n_{i\sigma}
			\rangle \hat n_{j\overline{\sigma}} + \hat n_{i\sigma} \langle 
			\hat n_{j\overline{\sigma}}
			\rangle
			- \langle 
			\hat n_{i\sigma}
			\rangle \langle 
			\hat n_{j\overline{\sigma}}
			\rangle
		\right]
	}_{\mathrm{o.s.}} \, +\, 
	(\text{all the rest})
	\label{eq:normal-V-wick-decomposition}
\end{multline}
where ``all the rest'' collects all non-Hartree terms. Recalling the Ansatz of Eq.~\eqref{eq:normal-density-ansatz}, we get
\[
	\hat H_V \simeq  \underbrace{
		-nV \sum_{\ev{ij}} \sum_\sigma \left[
		\hat n_{j\sigma} + \hat n_{i\sigma}
		\right]
	}_{\mathrm{s.s.}}
	\underbrace{
		-nV \sum_{\ev{ij}} \sum_\sigma \left[
		\hat n_{j\overline{\sigma}} + \hat n_{i\sigma}
		\right]
	}_{\mathrm{o.s.}} 
	- E_{\mathrm{H}/V}^{(\mathrm{N})}
	+ (\text{all the rest})
\]
with $E_{\mathrm{H}/V}^{(\mathrm{N})}$ the normal state shift to energy brought by $\hat H_V$. Both sums above give a number operator. Note that we sum over NNs: each site is connected to $z=4$ sites. Including this combinatorial factor, we have
\begin{equation}\label{eq:normal-HV-approx}
	\hat H_V \simeq -2nzV \times \hat N - E_{\mathrm{H}/V}^{(\mathrm{N})} + 
	(\text{all the rest})
\end{equation}
This accounts for the final Hartree shift of $\mu$,
\begin{equation}\label{eq:non-local-hartree-mu-shift}
	\mu \to \mu + 2znV
\end{equation}

Considering the net shift to $\mu$ due to both interactions by combining Eqns.~\eqref{eq:local-hartree-mu-shift}, \eqref{eq:non-local-hartree-mu-shift}, we get the final RMP anticipated in Tab.~\ref{tab:normal-RWCs},
\begin{equation}\label{eq:normal-mu-shift}
	\tilde{\mu} \equiv \mu + n(2zV-U)
\end{equation}
This result will remain valid throughout all phases discussed in this text. For the AF phase, Sec.~\ref{subsec:af-nl-hartree-analytic}, the SDW character leaves this shift untouched. The SC phase, Sec.~\ref{subsec:sc-hartree-analytic}, we will discuss is inherently translational invariant, thus the computation will be the same as here.

\subsection{Hartree contraction energy}\label{subsec:normal-hartree-energy}

The double counting terms accounting for energy shifts in the Hartree channel are, as anticipated:
\begin{align}
	E_{\mathrm{H}/U}^{(\mathrm{N})} &= \sum_i \langle 
		\hat n_{i\downarrow}
	\rangle \langle 
		\hat n_{i\uparrow}
	\rangle \nonumber \\
	&= L_x L_y \times n^2U \label{eq:normal-hartree-U-contraction-energy}
\end{align}
for the local repulsion and 
\begin{align}
	E_{\mathrm{H}/V}^{(\mathrm{N})} &= - V \sum_{\ev{ij}} \sum_\sigma \left[
		\langle 
			\hat n_{i\sigma}
		\rangle \langle 
			\hat n_{j\sigma}
		\rangle
		+
		\langle 
			\hat n_{i\sigma}
		\rangle \langle 
			\hat n_{j\overline{\sigma}}
		\rangle
	\right] \nonumber \\
	&= -2V \sum_{\ev{ij}} \sum_\sigma n^2 \nonumber \\
	&= - L_x L_y \times 2zn^2 V \label{eq:normal-hartree-V-contraction-energy}
\end{align}
for the non-local attraction. In the last passage, we used Eq.~\eqref{eq:square-NN-ghost-sum} and spin degeneracy. The contraction energy is an extensive quantity. When computing total free energy density, we will use the contraction energy per site.

\section{Fock effects: bare bands renormalisation}\label{sec:normal-fock-bands-renormalisation}

Analysis of Fock terms will lead us to the effect that makes the normal phase interesting, when compared to the free Fermi gas on the square lattice. From Wick's decomposition of $\hat H_V$, the unique allowed Fock term comes from the same-spin sector. This selection rules is implied by the $\mathrm{SU}(2)$ symmetry of the normal phase. Then, from Eq.~\eqref{eq:mft-HV}:
\begin{equation}\label{eq:real-space-non-local-interaction-fock}
	\hat H_V \simeq (\text{Hartree channel}) + 
	V \sum_{\ev{ij}} \sum_\sigma \left[
		\langle
		\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\rangle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} + 
		\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle - \langle \hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\rangle \langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle
	\right]
\end{equation}
Note the $+$ sign in front of the displayed term: it originates from the $-$ sign in Fock term of Eq.~\eqref{eq:hf-wick-hfc}. A bond-wise hopping amplitude can be defined,
\[
	\tilde{t}_{ij\sigma} \equiv t - V \langle
	\hat c_{j\sigma}^\dagger \hat c_{i\sigma}
	\rangle
\]
The effective kinetic hamiltonian is given by
\[
\begin{aligned}
	\hat H_{\tilde{t}} &= \hat H_t + V \sum_{\ev{ij}} \sum_\sigma \left[
	\langle
	\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\rangle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} + \hc
	\right] \\
	&= - \sum_{\ev{ij}} \sum_\sigma 
	\left[
	\tilde{t}_{ij\sigma} \hat c_{i\sigma}^\dagger \hat c_{j\sigma} + \hc
	\right]
\end{aligned}
\]
To proceed, it is useful to move to reciprocal space.

\subsection{Fock channel in the normal phase: analytic derivation}\label{subsec:normal-fock-analytic}

In reciprocal space, the effective hopping must be transformed as well. Consider the Fourier Transform $\mathcal{F}$ given in Eq.~\eqref{eq:HV-ss-fourier}, applied to the displayed part of Eq.~\eqref{eq:real-space-non-local-interaction-fock},
\begin{multline}
	V \sum_{\ev{ij}} \sum_\sigma \left[
		\langle
		\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\rangle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} + 
		\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle - \langle \hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\rangle \langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle
	\right] \\
	\stackrel{\mathcal{F}}{=} \frac{2V}{L_x L_y} \sum_{\mathbf{K}, \mathbf{k}, \mathbf{k}'} \sum_\sigma \left[
	\cos \left(
	\delta k_x
	\right)	+ \cos \left(
	\delta k_y
	\right)	
	\right]	
	\langle
	\hat c_{\mathbf{K}+\mathbf{k} \sigma}^\dagger 
	\hat c_{\mathbf{K}-\mathbf{k}' \sigma}
	\rangle
	\hat c_{\mathbf{K}-\mathbf{k} \sigma}^\dagger  \hat c_{\mathbf{K}+\mathbf{k}'\sigma} - E_{\mathrm{F}/V}^{(\mathrm{N})} \label{eq:normal-V-fock-int1}
\end{multline}
Here, the $2$ prefactor comes from recognizing that the first two operators in square brackets in the first line generate identical contributions to the full sum (as is easy to see by performing trivial variables changes); the double counting energy shift is given by
\begin{equation}\label{eq:normal-V-fock-double-counting}
	E_{\mathrm{F}/V}^{(\mathrm{N})} \equiv \frac{V}{L_x L_y} \sum_{\mathbf{K}, \mathbf{k}, \mathbf{k}'} \sum_\sigma \left[
		\cos \left(
			\delta k_x
		\right)	+ \cos \left(
			\delta k_y
		\right)	
	\right]	
	\langle
		\hat c_{\mathbf{K}+\mathbf{k} \sigma}^\dagger 
	\hat c_{\mathbf{K}-\mathbf{k}' \sigma}
	\rangle \langle
		\hat c_{\mathbf{K}-\mathbf{k} \sigma}^\dagger  \hat c_{\mathbf{K}+\mathbf{k}'\sigma} 
	\rangle
\end{equation}

Up to this point, this derivation is general and will be re-used in next chapters. Here the specificity of the physical phase settles in: if we assume the normal state to be a free degenerate Fermi gas with renormalized bands $\tilde{\epsilon}_\mathbf{k}$, we get
\begin{equation}\label{eq:normal-cc-EV}
	\langle \hat c_{\mathbf{k}_1\sigma_1}^\dagger \hat c_{\mathbf{k}_2 \sigma_2} \rangle = \delta_{\mathbf{k}_1=\mathbf{k}_2} \delta_{\sigma_1=\sigma_2} f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
\end{equation}
being $f$ the Fermi-Dirac distribution,
\[
	f(\epsilon;\beta,\mu) \equiv \frac{1}{e^{\beta(\epsilon-\mu)}+1}
\]
It follows that Eq.~\eqref{eq:normal-V-fock-int1} becomes
\begin{multline}
	V \sum_{\ev{ij}} \sum_\sigma \left[
		\langle
			\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\rangle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} + 
		\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\langle 
			\hat c_{j\sigma}^\dagger  \hat c_{i\sigma} 
		\rangle - \langle 
			\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
		\rangle \langle 
			\hat c_{j\sigma}^\dagger  \hat c_{i\sigma}
		\rangle
	\right] \\
	\stackrel{\mathcal{F}}{=} \frac{2V}{L_x L_y} \sum_{\mathbf{K}, \mathbf{k}} \sum_\sigma \left[
	\cos \left(
	2 k_x
	\right)	+ \cos \left(
	2 k_y
	\right)	
	\right]	
	\langle
	\hat c_{\mathbf{K}+\mathbf{k} \sigma}^\dagger 
	\hat c_{\mathbf{K}+\mathbf{k} \sigma}
	\rangle
	\hat c_{\mathbf{K}-\mathbf{k} \sigma}^\dagger  \hat c_{\mathbf{K}-\mathbf{k}\sigma} - E_{\mathrm{F}/V}^{(\mathrm{N})} \label{eq:normal-V-fock-int2}
\end{multline}
since the only contribution comes from $\mathbf{k}'=-\mathbf{k}$.

\begin{figure}
	\def\Kx{0.5}
	\def\Ky{0.2}
	\def\kx{0.1}
	\def\ky{0.7}
	\centering
	\subfloat[
		Generic vectors.
	]{
		\label{subfig:Kk-qq'-generic}
		\input{\GraphicsFolder/Kk}
	}
	\subfloat[
		Change of variables.
	]{
		\label{subfig:Kk-qq'-newvariables}
		\input{\GraphicsFolder/qq'}
	}
	\caption{Representation of the variable change employed to pass from Eq.~\eqref{eq:normal-V-fock-int2} to \eqref{eq:normal-V-fock-int3}. In Fig.~\ref{subfig:Kk-qq'-generic} generic vectors are considered, cycling over all values of $\mathbf{K},\mathbf{k}\in\mathrm{BZ}$. In Fig.~\ref{subfig:Kk-qq'-newvariables} is depicted the variables change to the new vectors $\mathbf{q},\mathbf{q}'$.}
	\label{fig:Kk-qq'}
\end{figure}

Let now $\mathbf{q}\equiv\mathbf{K}+\mathbf{k}$, $\mathbf{q}'\equiv\mathbf{K}-\mathbf{k}$. A representation of this variables change is given in Fig.~\ref{fig:Kk-qq'}. Being for $\ell=x,y$
\[
\begin{aligned}
	\delta q_\ell &\equiv q_\ell - q_\ell' \\
	&= (K_\ell+k_\ell) - (K_\ell-k_\ell) \\
	&= 2k_\ell
\end{aligned}
\]
we reduce Eq.~\eqref{eq:normal-V-fock-int1} to
\begin{multline}
	V \sum_{\ev{ij}} \sum_\sigma \left[
	\langle
	\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\rangle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} + 
	\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle - \langle \hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\rangle \langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle
	\right] \\
	\stackrel{\mathcal{F}}{=} \frac{2V}{L_x L_y} \sum_{\mathbf{q}, \mathbf{q}'} \sum_\sigma \left[
		\cos \left(
			\delta q_x
		\right)	+ \cos \left(
			\delta q_y
		\right)	
	\right]	
	\langle
		\hat c_{\mathbf{q}\sigma}^\dagger 
		\hat c_{\mathbf{q}\sigma}
	\rangle
	\hat c_{\mathbf{q}'\sigma}^\dagger  \hat c_{\mathbf{q}'\sigma} - E_{\mathrm{F}/V}^{(\mathrm{N})} \label{eq:normal-V-fock-int3}
\end{multline}
Recall now the result of the symmetry channels decomposition of Eq.~\eqref{eq:coscos-symmetries-decomposition},
\[
	\cos \left( \delta q_x \right)	+ \cos \left( \delta q_y  \right) = \frac 1 2 \sum_\gamma \varphi_\mathbf{q}^{(\gamma)} \varphi_{\mathbf{q}'}^{(\gamma)*}
	\qq{for $\gamma \in \lbrace s^*, p_x, p_y, d_{x^2-y^2} \rbrace$}
\]
Thanks to this handy property, we reduce Eq.~\eqref{eq:normal-V-fock-int3} to
\begin{multline}
	V \sum_{\ev{ij}} \sum_\sigma \left[
	\langle
	\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\rangle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} + 
	\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle - \langle \hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\rangle \langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle
	\right] \\
	\stackrel{\mathcal{F}}{=} \sum_{\gamma,\sigma} \left[
		\frac{1}{L_x L_y} \sum_\mathbf{q\in\mathrm{BZ}} \varphi_\mathbf{q}^{(\gamma)}
		u_{\mathbf{q}\sigma}
	\right] \times V
	\sum_{\mathbf{q}'\in\mathrm{BZ}}
	\varphi_{\mathbf{q}'}^{(\gamma)*}
	\hat c_{\mathbf{q}'\sigma}^\dagger
	\hat c_{\mathbf{q}'\sigma} - E_{\mathrm{F}/V}^{(\mathrm{N})} \label{eq:normal-V-fock-int4}
\end{multline}
where we defined the EV:
\begin{equation}\label{eq:normal-uqsigma-def}
	u_{\mathbf{q}\sigma} \equiv \langle
		\hat c_{\mathbf{q}\sigma}^\dagger
		\hat c_{\mathbf{q}\sigma}
	\rangle
	\qquad
	u_{\mathbf{q}\sigma} = u_{\mathbf{q}\sigma}^*
\end{equation}
The numeric value of $u_{\mathbf{q}\sigma}$ is given by the Fermi-Dirac distribution, but leaving it implicit we can re-use this computation later. Let $u^{(\gamma)}_\sigma$ be its $\gamma$-wave component,
\[
	u^{(\gamma)}_\sigma = \frac{1}{L_x L_y} \sum_{\mathbf{q}\in\mathrm{BZ}} \varphi_\mathbf{q}^{(\gamma)}
	\langle
	\hat c_{\mathbf{q}\sigma}^\dagger
	\hat c_{\mathbf{q}\sigma}
	\rangle
\]

The bare bands $\epsilon_\mathbf{k}$ are $s^*$-wave symmetric,
\[
	\epsilon_\mathbf{k} = -2t (\cos k_x + \cos k_y) = -4t \varphi_\mathbf{k}^{(s^*)}
\]
We used one function of those listed in Tab.~\ref{tab:x-wave-reciprocal-factors}. The renormalised bands $\tilde{\epsilon}_\mathbf{k}$ can exhibit a more complex symmetry. In the normal phase inversion symmetry is unbroken, thus
\[
	u_{\mathbf{q}\sigma} = u_{-\mathbf{q}\sigma}
\]
It follows that antisymmetric harmonics are zero,
\[
	u_\sigma^{(p_\ell)} = 0
	\qq{for}
	\ell\in\lbrace x,y \rbrace
\]
The remaining $s^*$ and $d$ symmetries give legitimate HFPs with the respective self-consistency equations, reported in Tab.~\ref{tab:normal-ehm-ugamma-self-consistencies}.Spin index is omitted due to spin degeneracy.

\begin{table}
	\centering
	\begin{tabular}{r E l l}
		\textbf{Symmetry} & \multicolumn{2}{c}{\textbf{Self-consistency equation}} & \textbf{Graph} \\
		\midrule
		$s^*$-wave & $u^{(s^*)}$ & $\displaystyle \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\cos k_x + \cos k_y) f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) $ & Fig.~\ref{subfig:s*-wave-correlator} \\[1.5em]
		$d_{x^2-y^2}$-wave & $u^{(d)}$ & $\displaystyle \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\cos k_x - \cos k_y) f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) $ & Fig.~\ref{subfig:d-wave-correlator} \\
		\midrule
	\end{tabular}
	\caption{Symmetry resolved self-consistency equations for the harmonics of $u_\mathbf{k}$.}
	\label{tab:normal-ehm-ugamma-self-consistencies}
\end{table}
\setlength{\extrarowheight}{0em}

The two HFPs $u_\sigma^{(s^*)}$ and $u_\sigma^{(d)}$ are in general non-zero, since we are allowing for $\mathrm{C}_4 \SSB \mathrm{C}_2$. Note that when imposing $\mathrm{C}_4$ invariance the $u_\sigma^{(d)}$ HFP vanishes, while in general $u_\sigma^{(s^*)}$ is non-zero. From these considerations Eq.~\eqref{eq:normal-V-fock-int4} becomes
\begin{multline}
	V \sum_{\ev{ij}} \sum_\sigma \left[
	\langle
	\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\rangle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} + 
	\hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle - \langle \hat c_{i\sigma}^\dagger \hat c_{j\sigma}
	\rangle \langle \hat c_{j\sigma}^\dagger  \hat c_{i\sigma} \rangle
	\right] \\
	\stackrel{\mathcal{F}}{=} V
	\sum_{\mathbf{q}\in\mathrm{BZ}} \sum_\sigma
	\left[
		u^{(s^*)}_\sigma
		\varphi_\mathbf{q}^{(s^*)*} + u^{(d)}_\sigma
		\varphi_\mathbf{q}^{(d)*}
	\right]
	\hat c_{\mathbf{q}\sigma}^\dagger
	\hat c_{\mathbf{q}\sigma} - E_{\mathrm{F}/V}^{(\mathrm{N})} \label{eq:normal-V-fock-int5}
\end{multline}
Let 
\begin{equation}\label{eq:normal-bands-renormalisation}
	\tilde{\epsilon}_{\mathbf{k}\sigma} \equiv \epsilon_{\mathbf{k}\sigma} + u^{(s^*)}_\sigma V (\cos k_x + \cos k_y) + u^{(d)}_\sigma V (\cos k_x - \cos k_y)
\end{equation}
This equation defines the bands renormalisation anticipated in Tab.~\ref{tab:normal-RWCs}, and is actually spin-independent. The fact that we have both $s^*$ and a $d$-wave components introduces an anisotropy that is discussed in next section. However, we anticipate here that, as will result from Sec.~\ref{subsec:normal-bands-shrinking-C2}, the $d$-wave harmonic will be suppressed. For now we keep this component ignoring these anticipations.

\subsection{Fock contraction energy}\label{subsec:normal-fock-energy}

Finally, let us derive the Fock contraction energy by expanding Eq.~\eqref{eq:normal-V-fock-double-counting}:
\begin{align}
	E_{\mathrm{F}/V}^{(\mathrm{N})} &= \frac{V}{L_x L_y} \sum_{ \mathbf{q}, \mathbf{q}'} \sum_\sigma \left[
		\cos \left(
			\delta q_x
		\right)	+ \cos \left(
			\delta q_y
		\right)	
	\right]	
	\langle
		\hat c_{\mathbf{q} \sigma}^\dagger \hat c_{\mathbf{q}' \sigma}
	\rangle \langle
		\hat c_{\mathbf{q} \sigma}^\dagger  \hat c_{\mathbf{q}' \sigma}
	\rangle \nonumber \\
	&= L_x L_y \times \frac{V}{2} \sum_{\gamma,\sigma} \left[
		\frac{1}{L_x L_y} \sum_{\mathbf{q}\in\mathrm{BZ}} \varphi_\mathbf{q}^{(\gamma)} u_{\mathbf{q}\sigma}
	\right] \left[
		\frac{1}{L_x L_y} \sum_{\mathbf{q}'\in\mathrm{BZ}} \varphi_{\mathbf{q}'}^{(\gamma)*} u_{\mathbf{q}'\sigma}^*
	\right] \nonumber \\
	&= L_x L_y \times \frac{V}{2} \sum_\sigma \left[
		\big|
			u_\sigma^{(s^*)}
		\big|^2 + \big|
			u_\sigma^{(d)}
		\big|^2
	\right] \label{eq:normal-fock-V-contraction-energy}
\end{align}
This expression gives the Fock contraction energy coming from the $\hat H_V$ operator. The squared spectral amplitude of $u_{\mathbf{k}\sigma}$ here is summed, and contributes to contraction energy linearly in $V$. This suggests that a mixed-symmetry band could be energetically preferred.

\subsection{Nematicity: extending hopping to $d$-wave bands}\label{subsec:normal-d-wave-hopping}

\begin{figure}
	\centering
	\input{\GraphicsFolder/anisotropic-hopping}
	\caption{Graphic rendition of anisotropic hopping, with a $s^*$-wave component (symmetric by $\pi/$ rotations) and a $d$-wave component (antisymmetric).}
	\label{fig:anisotropic-hopping}
\end{figure}

For the normal phase, there is no reason to expect nematic effects, nor to exclude them. They are observed in superconducting phase \cite{bohmer2022nematicity}. Thus, we allow the possibility.

From Tabs.~\ref{tab:x-wave-real-factors} and \ref{tab:x-wave-reciprocal-factors} we can inverse-Fourier transform the form factors to get the full effective hopping,
\[
	\tilde{t}_{ij\sigma} = \sum_{\gamma\in\lbrace s^*,d \rbrace} t^{(\gamma)}_\sigma \varphi_{ij}^{(\gamma)}
\]
with
\[
	t^{(s^*)}_\sigma = t - \frac{u_\sigma^{(s^*)} V}{2}
	\qquad
	t^{(d)}_\sigma = - \frac{u_\sigma^{(d)} V}{2}
\]
This is what we intend for ``nematic effects''. The possibility that planar rotational symmetry is broken in such a way that the kinetic operators in $x$ and $y$ directions differ. A schematics of anisotropic hopping is given in Fig.~\ref{fig:anisotropic-hopping}: for each bond linking two sites, the hopping parameter can be a positive or negative combination of the two components, whether the link direction is vertical or horizontal. The solution needs to be invariant under exchange $x \leftrightarrow y$, thus the actual SSB is $\mathrm{C}_4 \SSB \mathrm{C}_2 / \mathbb{Z}_2$.

The fact that hopping is here anisotropic does not break full translational invariance $\mathrm{T}_1^{(x)} \otimes \mathrm{T}_1^{(y)}$. Imposing a free degenerate Fermi gas ground state in Eq.~\eqref{eq:normal-cc-EV} leads to a uniform density. This is proven by Fourier-transforming the site number operator according to the convention of Eq.~\eqref{eq:fourier-convention},
\[
	\hat c_{\mathbf{r}\sigma}^\dagger \hat c_{\mathbf{r}\sigma} \stackrel{\mathcal{F}}{=} \frac{1}{L_x L_y} \sum_{\mathbf{k}, \mathbf{k}'} e^{i (\mathbf{k}-\mathbf{k}') \cdot\mathbf{r}} \hat c_{\mathbf{k}\sigma}^\dagger \hat c_{\mathbf{k}'\sigma}
\]
Then the on-site density EV is
\[
	\ev{\hat n_{\mathbf{r}\sigma}} = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
\]
A perfectly degenerate Fermi gas is always translational invariant and thus cannot exhibit non-uniform symmetry. The fact that hopping is allowed to be anisotropic does not interfere with that.

Note that for null HFPs,
\[
	u_\sigma^{(s^*)} = 0
	\qquad
	u_\sigma^{(d)} = 0
\]
we have, recalling Tab.~\ref{tab:x-wave-real-factors}:
\[
	\tilde{t}_{\mathbf{r}\mathbf{r}'\sigma} = \frac{t}{2} \left[
		\delta_{\mathbf{r}'=\mathbf{r}+\underline{\mathbf{x}}} + \delta_{\mathbf{r}'=\mathbf{r}-\underline{\mathbf{x}}} + \delta_{\mathbf{r}'=\mathbf{r}+\underline{\mathbf{y}}} + \delta_{\mathbf{r}'=\mathbf{r}-\underline{\mathbf{y}}}
	\right]
\]
The fact that $t/2$ appears here instead of $t$ is a consequence of the equivalence:
\[
	-t \sum_{\ev{ij}} \sum_\sigma \left[
		\hat c_{i\sigma}^\dagger \hat c_{j\sigma} + \hc
	\right] = -\frac{t}{2} \sum_{i,j \in \mathcal{S}} \sum_\sigma \left[
		\hat c_{i\sigma}^\dagger \hat c_{j\sigma} + \hc
	\right]
\]
where we including a prefactor that corrects double-counting. The RMP $\tilde{t}_{ij\sigma}$ must be interpreted like the right-hand side of the above equation, with the kinetic operator becoming:
\[
	\hat H_{\tilde{t}} \equiv -\frac{1}{2} \sum_{i,j \in \mathcal{S}} \sum_\sigma \left[
		\tilde{t}_{ij\sigma} \hat c_{i\sigma}^\dagger \hat c_{j\sigma} + \hc
	\right]
\]
To limit the sum to NNs is a matter of the symmetry structure of the function $\tilde{t}_{ij\sigma}$.

Recall the functions of Tab.~\ref{tab:x-wave-real-factors}. We have the identities:
\[
\begin{aligned}
	\delta_{\mathbf{r}'=\mathbf{r}+\underline{\mathbf{x}}} + \delta_{\mathbf{r}'=\mathbf{r}-\underline{\mathbf{x}}}&= \varphi_{\mathbf{r}\mathbf{r}'}^{(s^*)} + \varphi_{\mathbf{r}\mathbf{r}'}^{(d)} \\
	\delta_{\mathbf{r}'=\mathbf{r}+\underline{\mathbf{y}}} + \delta_{\mathbf{r}'=\mathbf{r}-\underline{\mathbf{y}}}&= \varphi_{\mathbf{r}\mathbf{r}'}^{(s^*)} - \varphi_{\mathbf{r}\mathbf{r}'}^{(d)}
\end{aligned}
\]
Then we can define the renormalised hopping on each direction as:
\[
\begin{aligned}
	\tilde{t}^{(x)} &\equiv t - \frac{u^{(s^*)} + u^{(d)}}{2} V \\
	\tilde{t}^{(y)} &\equiv t - \frac{u^{(s^*)} - u^{(d)}}{2} V
\end{aligned}
\]
Both components depend linearly on $V$. The HFP $u^{(s^*)}$ is a measure of the direction-averaged hopping shift with respect to $V$, while $u^{(d)}$ measures the anisotropy,
\begin{align}
	u^{(s^*)} &= \frac{
		\left(
		t-\tilde{t}^{(x)}
		\right) + \left(
		t-\tilde{t}^{(y)}
		\right)
	}{V} \label{eq:normal-us*(tx,ty)}
	\\
	u^{(d)} &= \frac{\tilde{t}^{(x)} - \tilde{t}^{(y)}}{V} \label{eq:normal-ud(tx,ty)}
\end{align}
It follows that if $u^{(d)}>0$, the hopping in direction $x$ is boosted while in direction $y$ is damped. If $u^{(d)}<0$, the contrary. 

This discussion is general, and not specific to the normal phase. In next chapters we will interpret phase renormalisation as in this section.

\section{Normal free energy density}\label{sec:normal-free-energy}

In the end, we want to compare phases. Thus we need a way to extract the free energy density of the approximated ground-state for each phase. The procedure sketched here will be re-used in next chapters. Let
\[
	f^{(\mathrm{N})} \equiv \frac{F_\mathrm{MFT}^{(\mathrm{N})}}{L_x L_y}
\]
being $F_\mathrm{MFT}$ the total free energy. Consider the complete hamiltonian
\[
	\hat H -\mu \hat N \simeq \sum_{\mathbf{k},\sigma} \tilde{\epsilon}_{\mathbf{k}\sigma} \hat c_{\mathbf{k}\sigma}^\dagger \hat c_{\mathbf{k}\sigma} + E_\mathrm{c}^{(\mathrm{N})} - \tilde{\mu} \hat N
\]
where $E_\mathrm{c}^{(\mathrm{N})}$ is the (negative) total contraction energy for the normal phase,
\[
	E_\mathrm{c}^{(\mathrm{N})} \equiv
	-\left[
		E_{\mathrm{H}/U}^{(\mathrm{N})} + E_{\mathrm{H}/V}^{(\mathrm{N})} +  E_{\mathrm{F}/V}^{(\mathrm{N})}
	\right]
\]
having included the various contraction energies from Eqns.~\eqref{eq:normal-hartree-U-contraction-energy}, \eqref{eq:normal-hartree-V-contraction-energy} and \eqref{eq:normal-fock-V-contraction-energy}.

Let us now take together all terms. From now on, we will omit spin index $\sigma$, making use of the always present $\mathbb{Z}_2$ spin inversion symmetry discussed in Sec.~\ref{sec:normal-phase-theory}. For a free Fermi gas, the free energy density is made of four contributions:
\begin{enumerate}
	\item The free particles ground state energy density,
	\[
		e_\mathrm{g}^{(\mathrm{N})} = \frac{2}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\tilde{\epsilon}_\mathbf{k}-\tilde{\mu}) f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu} )
	\]
	\item The contraction energy density,
	\[
	\begin{aligned}
		e_\mathrm{c}^{(\mathrm{N})} &\equiv \frac{E_\mathrm{c}^{(\mathrm{N})}}{L_x L_y} \\
		&= - n^2 U + V \left[
			2zn^2 - 
			\big|
				u^{(s^*)}
			\big|^2 - \big|
				u^{(d)}
			\big|^2
		\right]
	\end{aligned}
	\]
	\item The entropic contribution to free energy
	\[
		e_s^{(\mathrm{N})} \equiv -Ts
	\]
	with $T = 1/k_B \beta$ and $s$ the free Fermi gas entropy density for a single spin sector \cite{grosso2014solid},
	\begin{equation}\label{eq:normal-entropy-density}
		s = -\frac{k_B}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} \left[
			\left(
				1- f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu})
			\right) \ln \left(
				1- f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu})
			\right) + f(
				\tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu})
			\ln f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu})
			\vphantom{A^A}
		\right]
	\end{equation}
	We use the identity:
	\[
		\ln \left(
			1-\frac{1}{e^x+1}
		\right) = x+\ln \left(
			\frac{1}{e^x+1}
		\right) 
	\]
	and, substituting $x=\beta (\tilde{\epsilon}_\mathbf{k}-\tilde{\mu})$, reduce the above expression to:
	\begin{multline*}
		s = -\frac{k_B}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} \Big[ \ln \left(
		1- f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) \right) + f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) \ln f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) \\
		- \beta (\tilde{\epsilon}_\mathbf{k}-\tilde{\mu}) f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) - f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) \ln f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu})
		\Big]
	\end{multline*}
	Second and fourth terms cancel out, and multiplying by $-2T$ (taking care of spin degeneracy) we have
	\[
		e_s^{(\mathrm{N})} = \frac{2}{\beta L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} \ln \left(
		1- f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) \right) - \frac{2}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\tilde{\epsilon}_\mathbf{k}-\tilde{\mu}) f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu})
	\]
	\item The chemical potential correction
	\[
		e_n^{(\mathrm{N})} = \mu n
	\]
	Note here that we are using the ``physical'' value of $\mu$, not its RMP counterpart $\tilde{\mu}$. The reason for this is discussed in Sec.~\ref{subsec:fixed-density-role-mu}.
\end{enumerate}
Thus, composing everything
\[
\begin{aligned}
	f^{(\mathrm{N})} &\equiv e_\mathrm{g}^{(\mathrm{N})} + e_\mathrm{c}^{(\mathrm{N})} + e_s^{(\mathrm{N})} + e_n^{(\mathrm{N})} \nonumber \\
	&= \frac{2}{\beta L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} \ln \left(
	1- f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) \right) - V \left[ 
		\big|
			u^{(s^*)}
		\big|^2 + \big|
			u^{(d)}
		\big|^2
	\right] + \left[
		\mu + n(2zV-U)
	\right] n
\end{aligned}
\]
In Sec.~\ref{subsec:fixed-density-role-mu} we argued that our iterative scheme computes $\tilde{\mu}$, not $\mu$. Using Eq.~\eqref{eq:normal-mu-shift}, we see
\[
	\mu + n(2zV-U) = \tilde{\mu}
\]
Then, the final expression is:
\begin{equation}\label{eq:normal-fMFT}
	f^{(\mathrm{N})} = \frac{2}{\beta L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} \ln \left(
	1- f( \tilde{\epsilon}_\mathbf{k}; \beta,\tilde{\mu}) \right) - V \left[ 
		\big|
			u^{(s^*)}
		\big|^2 + \big|
			u^{(d)}
		\big|^2
	\right] + \tilde{\mu} n 
\end{equation}
including all effects.

\section{Theoretical constraints on the normal phase HFPs}\label{sec:normal-constraints}

{\color{tabred}[Move this section to appendix?]}

Within this section we discuss some theoretical constraints on the two HFPs of the normal phase. The discussion is divided in two parts: first we consider the simpler case of unviolated $\mathrm{C}_4$ symmetry, then we move to anisotropic hopping and $\mathrm{C}_4 \SSB \mathrm{C}_2$.

\begin{figure}[t]
	\centering
	\includegraphics[width=0.9\linewidth]{../Project/src/labs/bands/normal-nematic-bands.pdf}
	\caption{Theoretical representation of mixed-symmetry nematic bands for the normal phase. The white contour represent level lines. In top-left panel, the MBZ and NBZ are depicted. While these plots only account for positive-defined components, in text is described how to map each one on its counterpart with different sign.}
	\label{fig:normal-nematic-bands}
\end{figure}

Before starting, it is useful to spend a few words on the spatial structure of a mixed $s^* \otimes d$-wave band. Let in general for notational simplicity
\[
	\tilde{\epsilon}_\mathbf{k} = \epsilon^{(s^*)} (\cos k_x + \cos k_y) + \epsilon^{(d)} (\cos k_x - \cos k_y)
\]
It is the balance between the two components $\epsilon^{(s^*)}$ and $\epsilon^{(d)}$ that gives the main features of the renormalised band. Consider the $s^*$-wave and $d$-wave structure factors already presented in Fig.~\ref{fig:x-wave-reciprocal-factors}, and define the Magnetic Brillouin Zone (MBZ) and Nematic Brillouin Zone (NBZ) as follows:
\begin{align}
	\mathrm{MBZ} &\equiv \left\lbrace
	\mathbf{k}\in\mathrm{BZ}
	\mid 
	\varphi_\mathbf{k}^{(s^*)} \ge 0
	\right\rbrace \label{eq:MBZ-def} \\
	\mathrm{NBZ} &\equiv \left\lbrace
	\mathbf{k}\in\mathrm{BZ}
	\mid 
	\varphi_\mathbf{k}^{(d)} \ge 0
	\right\rbrace \label{eq:NBZ-def}
\end{align}
represented in the top-left panel of Fig.~\ref{fig:normal-nematic-bands}. The reason for the name of the MBZ will be clear in Chap.~\ref{chap:mft-af-instability}. In the rest of the panels of Fig.~\ref{fig:normal-nematic-bands} are reported examples of mixed symmetry bands, for positive components. The respective opposite sign plots are obtained by the following rules:
\[
\begin{aligned}
	\epsilon^{(s^*)} < 0 \qq{and} \epsilon^{(d)} < 0
	\quad&\implies\quad \text{Invert colors in Fig.~\ref{fig:normal-nematic-bands}} \\
	\epsilon^{(s^*)} > 0 \qq{and} \epsilon^{(d)} < 0
	\quad&\implies\quad \text{Rotate plots by $\pi/2$ in Fig.~\ref{fig:normal-nematic-bands}} \\
	\epsilon^{(s^*)} < 0 \qq{and} \epsilon^{(d)} > 0
	\quad&\implies\quad \text{Rotate plots by $\pi/2$ and invert colors in Fig.~\ref{fig:normal-nematic-bands}} \\
\end{aligned}
\]

\begin{figure}
	\centering
	\subfloat[
		MBZ sectors.
	]{
		\input{\GraphicsFolder/MBZ-sectors}
		\label{subfig:MBZ-sectors}
	}
	\subfloat[
		NBZ sectors.
	]{
		\input{\GraphicsFolder/NBZ-sectors}
		\label{subfig:NBZ-sectors}
	}
	\caption{Depiction of the sectors composing the MBZ (Fig.~\ref{subfig:MBZ-sectors}) and the NBZ (Fig.~\ref{subfig:NBZ-sectors}), based on the divisions shown in the top-left panel of Fig.~\ref{fig:normal-nematic-bands}. The full green zone composes $\mathcal{A}$, the region where both the $s^*$-wave and $d$-wave structure factors are positive defined; its MBZ complementary white counterpart in the left panel gives $\mathcal{B}$, for which the $s^*$-wave structure factors is positive-definite while the $d$-wave is negative. Similarly the NBZ complementary white counterpart in the right panel gives $\mathcal{C}$, for which signs are exchanged. The black dots, at the contours intersection, are band nodes.}
	\label{fig:BZ-sectors}
\end{figure}

Let us focus on the MBZ and the NBZ. Starting from the intersections of the two zones, both can be factored in two sectors of interest,
\[
	\mathrm{MBZ} = \mathcal{A} \cup \mathcal{B}
	\qquad
	\mathrm{NBZ} = \mathcal{A} \cup \mathcal{C}
\]
being, accordingly to Fig.~\ref{fig:BZ-sectors},
\[
	\mathcal{A} \equiv \mathcal{A}_1 \cup \mathcal{A}_2
	\qquad
	\mathcal{B} \equiv \mathcal{B}_1 \cup \mathcal{B}_2
	\qquad
	\mathcal{C} \equiv \mathcal{C}_1 \cup \mathcal{C}_2
\]
These domains satisfy:
\begin{align}
	\mathcal{A} &\equiv \left\lbrace
	\mathbf{k}\in\mathrm{BZ}
	\mid 
	\varphi_\mathbf{k}^{(s^*)} \ge 0 \land 	\varphi_\mathbf{k}^{(d)} \ge 0
	\right\rbrace \label{eq:BZ-A-def} \\
	&= \mathrm{MBZ} \cap \mathrm{NBZ} \nonumber\\[0.5em]
	\mathcal{B} &\equiv \left\lbrace
	\mathbf{k}\in\mathrm{BZ}
	\mid 
	\varphi_\mathbf{k}^{(s^*)} \ge 0 \land 	\varphi_\mathbf{k}^{(d)} \le 0
	\right\rbrace \label{eq:BZ-B-def} \\
	&= \mathrm{MBZ} \setminus \mathrm{NBZ} \nonumber\\[0.5em]
	\mathcal{C} &\equiv \left\lbrace
	\mathbf{k}\in\mathrm{BZ}
	\mid 
	\varphi_\mathbf{k}^{(s^*)} \le 0 \land 	\varphi_\mathbf{k}^{(d)} \ge 0
	\right\rbrace \label{eq:BZ-C-def} \\
	&= \mathrm{NBZ} \setminus \mathrm{MBZ} \nonumber
\end{align}
and will be used later on. Whatever the shape of the mixed band, the following equation always holds
\[
	\abs{k_x} = \frac{\pi}{2} \qq{and} \abs{k_y} = \frac{\pi}{2}
	\quad\implies\quad
	\tilde{\epsilon}_{(k_x,k_y)} = 0
\]
since only cosines are involved. This identifies four node points, represented as black dots in Fig.~\ref{fig:BZ-sectors}.

\subsection{Constraints in presence of a robust $\mathrm{C}_4$ symmetry and bands shrinking}\label{subsec:normal-bands-shrinking}

Consider the simpler situation where $\mathrm{C}_4$ symmetry is imposed. This implies that the renormalised bands are
\[
	\tilde{\epsilon}_\mathbf{k} \equiv (u^{(s^*)} V - 2t) (\cos k_x + \cos k_y)
\]
with a single self-consistency equation
\[
	u^{(s^*)} = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\cos k_x + \cos k_y) f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
\]
The following chain of deductions leads us to a constraint on the possible values of $u^{(s^*)}$:
\begin{enumerate}
	\item For $t>0$, there is no self-consistent solution admitting $u^{(s^*)}\le0$. Let us prove this: suppose instead $u^{(s^*)}\le0$. For any function $g_\mathbf{k}$ defined on the BZ:
	\[
		\sum_{\mathbf{k}\in\mathrm{BZ}} g_\mathbf{k} = \sum_{\mathbf{k}\in\mathrm{MBZ}} \left[
		g_\mathbf{k} + g_{\mathbf{k}+\bm{\pi}}
		\right]
	\]
	where we defined the vector $\bm{\pi}\equiv(\pi,\pi)$. The self-consistency equation thus reads
	\begin{align}
		u^{(s^*)} &= \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{MBZ}} (\cos k_x + \cos k_y) \left[
			f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) + f(\tilde{\epsilon}_{\mathbf{k}+\bm{\pi}};\beta,\tilde{\mu})
		\right] \nonumber \\
		&= \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{MBZ}} (\cos k_x + \cos k_y) \left[
			f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
		\right] \label{eq:normal-us*-constraint-int1}
	\end{align}
	having we used the antisymmetry of $s^*$-wave functions by $\bm{\pi}$ shifts. Now, if $u^{(s^*)}\le0$ the renormalised band $\tilde{\epsilon}_\mathbf{k}$ is negative inside the MBZ and positive outside, implying
	\[
		\forall \mathbf{k} \in \mathrm{MBZ}
		\quad\colon\quad
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) \ge f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
	\]
	the equality only being satisfied on the MBZ contour. Then, since inside the MBZ it holds by definition $\cos k_x + \cos k_y \ge 0$, the above equations imply
	\[
		u^{(s^*)} \le 0 
		\quad\implies\quad
		u^{(s^*)} \ge 0
	\]
	To exclude the possibility of $u^{(s^*)}=0$, it suffices to notice that from the argument above such a situation only arises when the MBZ contour degenerates to the entirety of the MBZ itself -- which is, when the band is flat. But that cannot be the case for $u^{(s^*)} = 0$, as long as $t>0$. Thus we have proven that
	\[
		u^{(s^*)} > 0
	\]
	\item For $t>0$, there is no self-consistent solution admitting $u^{(s^*)} \ge 2t/V$. We proceed exactly as did for the previous point, up to the passage:
	\[
		u^{(s^*)} = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{MBZ}} (\cos k_x + \cos k_y) \left[
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
		\right]
	\]
	Now, if $u^{(s^*)} \ge 2t/V$ this means the prefactor in front of the renormalised bands is positive. Then $\tilde{\epsilon}_\mathbf{k}$ is positive inside the MBZ and negative outside, implying
	\[
		\forall \mathbf{k} \in \mathrm{MBZ}
		\quad\colon\quad
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) \le f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
	\]
	thus we have
	\[
		u^{(s^*)} \ge 2t/V
		\quad\implies\quad
		u^{(s^*)} \le 0
	\]
	which is absurd. Thus
	\[
		u^{(s^*)} < 2t/V
	\]
\end{enumerate}
Collecting the two points above, we have the constraint on $u^{(s^*)}$
\begin{equation}\label{eq:normal-us*-constraint}
	\mathrm{C}_4
	\quad\implies\quad
	0 < u^{(s^*)} < 2t/V
\end{equation}
The effective hopping is smaller than the original hopping. We can denominate this net effect ``bands shrinking''.

\subsection{Natural suppression of nematicity within $\mathrm{C}_2/\mathbb{Z}_2$ symmetry}\label{subsec:normal-bands-shrinking-C2}

Let us extend the arguments above to a weaker symmetry, allowing for
\[
	\mathrm{C}_4 \SSB \mathrm{C}_2/\mathbb{Z}_2
\]
%
\begin{figure}[t]
	\centering
	\subfloat[Same-sign mixed bands.]{
		\includegraphics[width=0.45\linewidth]{../Project/src/labs/bands/normal-nematic-bands-pp.pdf}
		\label{subfig:normal-nematic-bands-detail-pp}
	}
	\hspace{0.5em}
	\subfloat[Opposite-sign mixed bands.]{
		\includegraphics[width=0.45\linewidth]{../Project/src/labs/bands/normal-nematic-bands-mp.pdf}
		\label{subfig:normal-nematic-bands-detail-mp}
	}
	\caption{Theoretical example of bands with $u^{(s^*)} > 2t/V$, $u^{(d)}>0$ (top-left) and $u^{(s^*)} < 0$, $u^{(d)}>0$ (top right). Two example points are reported, connected by a $\pi/2$ rotation as given in Eqns.~\eqref{eq:normal-A=RB}, \eqref{eq:normal-B=RA}.}
	\label{fig:normal-nematic-bands-detail}
\end{figure}
%
We work with the full, anisotropic band of Eq.~\eqref{eq:normal-bands-renormalisation}. From the following chain of deductions we conclude that $\mathrm{C}_4$ is not broken:
\begin{enumerate}
	\item Let for instance
	\[
		u^{(s^*)} \ge 2t/V > 0
		\qquad
		u^{(d)} > 0
		\quad\implies\quad
		\epsilon^{(s^*)} \ge 0
		\qquad
		\epsilon^{(d)} > 0
	\]
	In Fig.~\ref{subfig:normal-nematic-bands-detail-pp} an example band is shown for $\epsilon^{(s^*)}=1$, $\epsilon^{(d)}=1/2$. Note that if $u^{(s^*)}=2t/V$, the renormalised band is purely $d$-wave since $\epsilon^{(s^*)}=0$. 
	
	Recall Eq.~\eqref{eq:normal-us*-constraint-int1}, and separate in two sums over $\mathcal{A}$ and $\mathcal{B}$:
	\begin{multline*}
		u^{(s^*)} = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathcal{A}} (\cos k_x + \cos k_y) \left[
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
		\right] \\
		+ \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathcal{B}} (\cos k_x + \cos k_y) \left[
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
		\right]
	\end{multline*}
	
	Consider the sum over $\mathcal{B}$. Looking at the bottom-left panel of Fig.~\ref{fig:normal-nematic-bands}, such a region contains nodal curves across which bands change sign.	
	Let $\mathcal{R}$ be the clockwise $\pi/2$ rotation: then, any point in $\mathcal{B}$ can be seen as the rotation of a point in $\mathcal{A}$
	\begin{equation}\label{eq:normal-B=RA}
		\forall \mathbf{k}' \in \mathcal{B} 
		\quad\exists\quad \mathbf{k} \in \mathcal{A} \mid \mathbf{k}' = \mathcal{R} \mathbf{k}
	\end{equation}
	A graphical rendition of this idea is given in Fig.~\ref{subfig:normal-nematic-bands-detail-pp}. Then we can rewrite $u^{(s^*)}$ as a single sum over $\mathcal{A}$,
	\begin{multline}
		u^{(s^*)} = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathcal{A}} (\cos k_x + \cos k_y) \\
		\times \left[
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) + f(\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu})
		\right]
		\label{eq:normal-us*-AR}
	\end{multline}
	
	From Eq.~\eqref{eq:BZ-A-def}, $\forall \mathbf{k} \in \mathcal{A}$ the form factors $\cos k_x \pm \cos k_y$ are positive by construction. By hypothesis $\epsilon^{(s^*)} \ge 0$ and $\epsilon^{(d)} > 0$. Hence:
	\[
	\forall \mathbf{k} \in \mathcal{A}
	\quad\colon\quad
	\begin{aligned}
		\tilde{\epsilon}_\mathbf{k} &= \overbrace{\epsilon^{(s^*)} \left( \cos k_x + \cos k_y \right)}^{\ge 0} + \overbrace{\epsilon^{(d)}  \left( \cos k_x - \cos k_y \right) }^{\ge 0} \\
		\tilde{\epsilon}_{\mathcal{R}\mathbf{k}} &= \underbrace{\epsilon^{(s^*)} \left( \cos k_x + \cos k_y \right)}_{\ge 0} - \underbrace{\epsilon^{(d)}  \left( \cos k_x - \cos k_y \right) }_{\ge 0}
	\end{aligned}
	\]
	Summing the two equations above we have:
	\[
	\forall \mathbf{k} \in \mathcal{A}
	\quad\colon\quad
	\tilde{\epsilon}_\mathbf{k} + \tilde{\epsilon}_{\mathcal{R}\mathbf{k}} \ge 0
	\]
	thus $\tilde{\epsilon}_\mathbf{k} \ge -\tilde{\epsilon}_{\mathcal{R}\mathbf{k}}$.	The Fermi-Dirac distribution is monotonically descendent. Then:
	\[
	\forall \mathbf{k} \in \mathcal{A}
	\quad\colon\quad
	\begin{aligned}
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) &\le f(-\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu}) \\
		f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) &\ge f(\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu}) 
	\end{aligned}
	\]
	This condition, inserted in Eq.~\eqref{eq:normal-us*-AR}, leads to an absurd:
	\[
		u^{(s^*)} \ge 2t/V \qquad
		u^{(d)} > 0
		\quad\implies\quad
		u^{(s^*)} \le 0
	\]
	%
	\item An identical argument, exchanging the roles of $\mathcal{A}$ and $\mathcal{B}$, can be carried out to show that
	\[
		u^{(s^*)} \ge 2t/V > 0
		\qquad
		u^{(d)} < 0
		\quad\implies\quad
		u^{(s^*)} \le 0
	\]
	%
	\item The two points above imply that it is possible to have $u^{(s^*)} \ge 2t/V$ if and only if $u^{(d)}=0$. But from Sec.~\ref{subsec:normal-bands-shrinking}, we conclude that the upper boundary $u^{(s^*)} < 2t/V$ always holds.
	%
	\item Let for instance
	\[
		u^{(s^*)} \le 0
		\qquad
		u^{(d)} > 0
		\quad\implies\quad
		\epsilon^{(s^*)} < 0
		\qquad
		\epsilon^{(d)} > 0
	\]
	In Fig.~\ref{subfig:normal-nematic-bands-detail-mp} an example band is shown for $\epsilon^{(s^*)}=-1$, $\epsilon^{(d)}=1/2$.
	
	Proceed as for the first point. Similarly to Eq.~\eqref{eq:normal-B=RA}, Any point in $\mathcal{A}$ can be obtained by rotating some point in $\mathcal{B}$,
	\begin{equation}\label{eq:normal-A=RB}
		\forall \mathbf{k}' \in \mathcal{A} 
		\quad\exists\quad \mathbf{k} \in \mathcal{B} \mid \mathbf{k}' = \mathcal{R} \mathbf{k}
	\end{equation}
	An equation similar to Eq.~\eqref{eq:normal-us*-AR} holds, the sum being performed over $\mathcal{B}$
	\begin{multline}
		u^{(s^*)} = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathcal{B}} (\cos k_x + \cos k_y) \\
		\times \left[
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) + f(\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) - f(-\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu})
		\right]
		\label{eq:normal-us*-BR}
	\end{multline}
	
	From Eq.~\eqref{eq:BZ-B-def}, $\forall \mathbf{k} \in \mathcal{B}$ the form factor $\cos k_x + \cos k_y$ is positive by construction; the form factor $\cos k_x - \cos k_y$ is negative by construction. By hypothesis $\epsilon^{(s^*)} < 0$ and $\epsilon^{(d)} > 0$. Hence:
	\[
		\forall \mathbf{k} \in \mathcal{B}
		\quad\colon\quad
		\begin{aligned}
			\tilde{\epsilon}_\mathbf{k} &= \overbrace{\epsilon^{(s^*)} \left( \cos k_x + \cos k_y \right)}^{\le 0} + \overbrace{\epsilon^{(d)}  \left( \cos k_x - \cos k_y \right) }^{\le 0} \\
			\tilde{\epsilon}_{\mathcal{R}\mathbf{k}} &= \underbrace{\epsilon^{(s^*)} \left( \cos k_x + \cos k_y \right)}_{\le 0} - \underbrace{\epsilon^{(d)}  \left( \cos k_x - \cos k_y \right) }_{\le 0}
		\end{aligned}
	\]
		Summing the two equations above we have:
	\[
		\forall \mathbf{k} \in \mathcal{B}
		\quad\colon\quad
		\tilde{\epsilon}_\mathbf{k} + \tilde{\epsilon}_{\mathcal{R}\mathbf{k}} \le 0
	\]
	thus $\tilde{\epsilon}_\mathbf{k} \le -\tilde{\epsilon}_{\mathcal{R}\mathbf{k}}$.	The Fermi-Dirac distribution is monotonically descendent. Then:
	\[
	\forall \mathbf{k} \in \mathcal{B}
	\quad\colon\quad
	\begin{aligned}
		f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) &\ge f(-\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu}) \\
		f(-\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) &\le f(\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu}) 
	\end{aligned}
	\]
	This condition, inserted in Eq.~\eqref{eq:normal-us*-AR}, leads to an absurd:
	\[
		u^{(s^*)} \le 0 \qquad
		u^{(d)} > 0
		\quad\implies\quad
		u^{(s^*)} > 0
	\]
	%
	\item can be carried out to show that
	\[
		u^{(s^*)} \le 0
		\qquad
		u^{(d)} < 0
		\quad\implies\quad
		u^{(s^*)} > 0
	\]
	%
	\item The two points above imply that it is possible to have $u^{(s^*)} \le 0$ if and only if $u^{(d)}=0$. But from Sec.~\ref{subsec:normal-bands-shrinking}, we conclude that the lower boundary $u^{(s^*)} > 0$ always holds.
	\newcounter{enumTemp}
	\setcounter{enumTemp}{\theenumi}
\end{enumerate}
From the chain of derivations above, we can conclude self-consistently that also in the nematic structure the constraint of Eq.~\eqref{eq:normal-us*-constraint},
\[
	0 < u^{(s^*)} < 2t/V
\]
holds. In the $s^*$-wave channel, partial breaking of rotational symmetry is not enough to modify the main effect identified under $\mathrm{C}_4$ symmetry, ``bands shrinking''. What is missing is the behaviour of the $d$-wave channel when said effect takes place. Continuing the enumeration above,

\begin{enumerate}
	\setcounter{enumi}{\theenumTemp}
	\item Let $0 < u^{(s^*)} < 2t/V$ and $u^{(d)} > 0$. Then $\epsilon^{(d)}>0$. The self-consistency equation for $u^{(d)}$ reads
	\[
	\begin{aligned}
		u^{(d)} &= \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\cos k_x - \cos k_y) f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) \\
		&= \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{NBZ}} (\cos k_x - \cos k_y) \left[
			f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) - f(\tilde{\epsilon}_{\mathcal{R}\mathbf{k}};\beta,\tilde{\mu})
		\right]
	\end{aligned}
	\]
	where we recognised, as is shown in Fig.~\ref{subfig:NBZ-sectors}, that $\mathrm{BZ} \setminus \mathrm{NBZ} = \mathcal{R} \mathrm{NBZ}$. It holds:
	\[
		\forall \mathbf{k} \in \mathrm{NBZ}
		\quad\colon\quad
		\begin{aligned}
			\tilde{\epsilon}_\mathbf{k} &= \epsilon^{(s^*)} \left( \cos k_x + \cos k_y \right) + \overbrace{\epsilon^{(d)}  \left( \cos k_x - \cos k_y \right) }^{\ge 0} \\
			\tilde{\epsilon}_{\mathcal{R}\mathbf{k}} &= \epsilon^{(s^*)} \left( \cos k_x + \cos k_y \right) - \underbrace{\epsilon^{(d)}  \left( \cos k_x - \cos k_y \right) }_{\ge 0}
		\end{aligned}
	\]
	thus $\tilde{\epsilon}_{\mathcal{R}\mathbf{k}} < \tilde{\epsilon}_\mathbf{k}$, and inserting this fact in the self-consistency equation above we get
	\[
		0 < u^{(s^*)} < 2t/V
		\qq{and}
		u^{(d)} > 0
		\quad\implies\quad
		u^{(d)} < 0
	\]
	which is absurd.
	%
	\item An identical argument, with inverted inequalities, can be carried out to show that
	\[
		0 < u^{(s^*)} < 2t/V
		\qq{and}
		u^{(d)} < 0
		\quad\implies\quad
		u^{(d)} > 0
	\]
	%
	\item The two points above imply that it is possible to satisfy the constraint $0 < u^{(s^*)} < 2t/V$ if and only if $u^{(d)}=0$.
\end{enumerate}
All the deductions above indicate that $u^{(d)}=0$ everywhere. Nematic effects are always absent, 
\begin{equation}\label{eq:normal-us*-ud-constraint}
	0 < u^{(s^*)} < 2t/V
	\qq{and}
	u^{(d)} = 0
\end{equation}
This is physically expected but non trivial. The normal phase does not break $\mathrm{C}_4$ and we can ignore $u^{(d)}$ at all. In next section we identify the expected self-consistent value for $u^{(s^*)}$.

\subsection{Expected dependence on $\delta$ at low temperature}\label{subsec:normal-uS-doping}

We understood that $u^{(d)}=0$. Moreover, the unique HFP $u^{(s^*)}$ is independent of $U$, but in general it will depend in some way on the doping level. We work at low-temperature, $\beta \to \infty$. Consider the self-consistency equation of Tab.~\ref{tab:normal-ehm-ugamma-self-consistencies} as a function of temperature and chemical potential,
\[
	u^{(s^*)}(\beta,\tilde{\mu}) = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\cos k_x + \cos k_y) f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
\]
in the zero-temperature limit, as long as the band is not flat, it holds
\[
	\lim_{\beta \to \infty} u^{(s^*)}(\beta,\tilde{\mu}) = \frac{1}{L_x L_y} \sum_{\epsilon_\mathbf{k} \le \tilde{\mu}} (\cos k_x + \cos k_y)
\]
Let $\mathrm{FS}(\delta)$ be the Fermi surface for a given doping level $\delta$. We denote with $\mathrm{Int}(\mathrm{FS})$ be its internal part, the Fermi sea:
\[
\begin{aligned}
	\mathrm{FS} = \left\lbrace \mathbf{k} \in \mathrm{BZ} \mid \tilde{\epsilon}_\mathbf{k} = \tilde{\mu} \right\rbrace \\
	\mathrm{Int}(\mathrm{FS}) = \left\lbrace \mathbf{k} \in \mathrm{BZ} \mid \tilde{\epsilon}_\mathbf{k} \le \tilde{\mu} \right\rbrace
\end{aligned}
\]

At half filling, the Fermi surface coincides with the boundary of the MBZ:
\[
	\delta = 0
	\quad\implies\quad
	\mathrm{FS} = \partial \mathrm{MBZ}
\]
This is implied by the fact that renormalised bands are antisymmetric by $\bm{\pi}$ shifts,
\[
	\tilde{\epsilon}_{\mathbf{k}\sigma} = -\tilde{\epsilon}_{\mathbf{k}+\bm{\pi}\sigma}
\]
The portion of an $s^*$-wave band outside the MBZ is mirrored with respect to the portion inside the MBZ. This implies that half the states lie in the MBZ, half out.

At $\delta>0$ the FS expands in the region $\mathrm{BZ}\setminus\mathrm{MBZ}$ where the form factor $\cos k_x + \cos k_y$ becomes negative. This gives the self-consistency equation
\[
	\lim_{\beta \to \infty} u^{(s^*)}[\beta,\tilde{\mu}] \simeq \frac{1}{L_x L_y} \sum_{\epsilon_\mathbf{k} \le 0} \abs{\cos k_x + \cos k_y} - \frac{1}{L_x L_y} \sum_{0<\epsilon_\mathbf{k} \le \tilde{\mu}} \abs{\cos k_x + \cos k_y}
\]
which, being the sum of a positive and a negative term, is maximised whenever $\tilde{\mu}=0$, at half filling. This holds as long as the band is not flat. Indeed, if the effective hopping is not zero, to rise the doping level means to 
\[
	\delta_1 > \delta_2
	\quad\implies\quad
	\mathrm{Int}(\mathrm{FS}_1) \subset \mathrm{Int}(\mathrm{FS}_2)
\]
with obvious meaning of the notation. Using this property we conclude
\[
	\tilde{t} > 0
	\qq{and}
	\delta_1 > \delta_2
	\quad\implies\quad
	\lim_{\beta \to \infty} u^{(s^*)}[\beta,\tilde{\mu}] \Big|_{\delta_1} < 
	\lim_{\beta \to \infty} u^{(s^*)}[\beta,\tilde{\mu}] \Big|_{\delta_2}
\]
This is the first expected behaviour of the self-consistent functional $u^{(s^*)}$: if there exists a region of parametric space where the shrinking effect is not strong enough to flatten the band, a rise in the doping level must lower $u^{(s^*)}$. Such a region must exist: at least all the segment $(V=0,\delta)$ is included, since on said region of parametric space the model is just the common free electronic model.

Following this line of reasoning, we can extract the two low-temperature limits of $u^{(s^*)}$ at half-filling and unitary filling.
\begin{enumerate}
	\item At half-filling, the self-consistency equation reduces to
	\[
		u^{(s^*)}[\beta,0] \big|_{\beta \gg 1} \simeq \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{MBZ}} (\cos k_x + \cos k_y)
	\]
	In thermodynamic limit, performing integration on the MBZ shown in the top-left panel of Fig.~\ref{fig:normal-nematic-bands}:
	\begin{align}
		\lim_{\beta \to \infty} u^{(s^*)}[\beta,0] &\simeq \frac{1}{(2\pi)^2} \int_{-\pi}^{\pi} \mathrm{d} k_y \int_{-\pi+\abs{k_y}}^{\pi-\abs{k_y}} \mathrm{d} k_x \cos k_x + \frac{1}{(2\pi)^2} \int_{-\pi}^{\pi} \mathrm{d} k_x \int_{-\pi+\abs{k_x}}^{\pi-\abs{k_x}} \mathrm{d} k_y \cos k_y \nonumber \\
		&= \frac{1}{2\pi^2} \int_{-\pi}^{0} \mathrm{d} k_y \int_{-\pi-k_y}^{\pi+k_y} \mathrm{d} k_x \cos k_x + \frac{1}{2\pi^2} \int_{0}^{\pi} \mathrm{d} k_y \int_{-\pi+k_y}^{\pi-k_y} \mathrm{d} k_x \cos k_x \nonumber \\
		&= \frac{1}{\pi^2} \int_{-\pi}^{0} \mathrm{d} k_y \sin (\pi+k_y) + \frac{1}{\pi^2} \int_{0}^{\pi} \mathrm{d} k_y \sin (\pi-k_y) \nonumber \\
		&= \left( \frac{2}{\pi} \right)^2 \approx 0.405 \label{eq:normal-uS-half-filling}
	\end{align}
	\item At unitary filling we have
	\[
		\lim_{\beta \to \infty} u^{(s^*)}\left[
			\beta,2\tilde{t}^{(s^*)}
		\right] = \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\cos k_x + \cos k_y) = 0
	\]
	which vanishes due to the structure factor periodicity by $\bm{\pi}$.
\end{enumerate}
To summarise: around $V=0$, $u^{(s^*)}$ is expected to descend monotonically from $4/\pi^2$ to $0$ at increasing doping.

\section{Discussion of numeric results}\label{sec:normal-numeric}

In this section we present and discuss the numeric results, the physical relevance of the found phenomena and the boundaries of the algorithm. Let us start by sketching the algorithm used to determine self-consistently the mean-field structure of the normal phase in parameter space. For the normal phase, the HFPs ``vector'' defined in Sec.~\ref{subsec:reject-mix-iterate} has just one component:
\[
	\mathbf{v} = \begin{bmatrix}
		u^{(s^*)}
	\end{bmatrix}
\]
As before, we treat spin as a pure degeneracy. The only HFP is characterised by the first self-consistency equation of Tab.~\ref{tab:normal-ehm-ugamma-self-consistencies}. The resulting model has two RMPs:
\[
\begin{aligned}
	\tilde{t}_{ij} &\equiv \left(
		t - \frac{u^{(s^*)}V}{2}
	\right) \varphi_{ij}^{(s^*)} \\
	\tilde{\mu} &\equiv \mu + n (2zV-U)
\end{aligned}
\]
the second one being only dependent on the model parameters and not on HFPs. 
Finally, the phase free energy is given by Eq.~\eqref{eq:normal-fMFT}. The normal phase is completely independent of $U$. Because of this we present the results varying the other parameters, keeping everywhere $U=0$. We ran simulations in the $(V, \delta)$ plane, with the setup:
\[
	U=0
	\qquad
	\Delta n = 10^{-2}
	\qquad
	\Delta \mathbf{v} = \begin{bmatrix}
		u^{(s^*)} \colon 5 \times 10^{-4}
	\end{bmatrix}
\]
with $\Delta \mathbf{v}$ the tolerance vector defined as in Sec.~\ref{subsec:reject-mix-iterate}.

\begin{figure}
	\centering
	\subfloat[Heatmap of $u^{(s^*)}$ in $\delta V$ plane.]{
		\includegraphics[height=0.32\linewidth]{../Project/plots/refined/Mode=rs/Phase=Normal/Setup=B[128]/Syms=/RB=S/Obj=HFPs/xVar=δ_yVar=V/uS_t=1.0_U=0.0_L=128_β=100.0.pdf}
		\label{subfig:normal-uS-data-B128}
	}
	\subfloat[$V$-dependency of $u^{(s^*)}$ varying $\delta$.]{
		\includegraphics[height=0.32\linewidth]{../Project/plots/refined/Mode=hs/Phase=Normal/Setup=B[256]/Syms=/RB=S/Obj=HFPs/xVar=V_pVar=δ/uS_t=1.0_U=0.0_L=256_β=100.0.pdf}
		\label{subfig:normal-uS-data-B256}
	}
	\caption{Plot of the self-consistent convergence values for the HFP $u^{(s^*)}$ in the normal phase. To extract the data on the right side coarse-graining precision of the left-hand side data has been exchanged with higher lattice dimensions. As is evident, based on doping level $u^{(s^*)}$ exhibits non-trivial regions of $V$-independence.}
	\label{fig:normal-uS-data}
\end{figure}

In Fig.~\ref{subfig:normal-uS-data-B128} are reported the results obtained for the HFP $u^{(s^*)}$ on a $128 \times 128$ lattice and ``low'' temperature $\beta=100$, varying the non-local attraction and the doping level
\[
	V = 0,~0.1,~0.2,~\dots,~4.0
	\qquad
	\delta = 0,~0.01,~0.02,~\dots,~0.49
\]
The expectations of Sec.~\ref{subsec:normal-uS-doping} are satisfied: at low $V$ (indicatively $V \lesssim 5$) the trend is monotonically descendent at increasing doping. As $V$ gets bigger, however, $u^{(s^*)}$ starts decreasing in the top-left corner of Fig.~\ref{subfig:normal-uS-data-B128}. This effect is more visible in Fig.~\ref{subfig:normal-uS-data-B256}, where we doubled $L$ and ran simulations with a smaller $\delta$ resolution: the convergence value of $u^{(s^*)}$ is approximately independent of $V$ up to some point, where this behaviour is broken. Note that, as was expected from the discussion of Sec.~\ref{subsec:normal-uS-doping}, the half-filled convergence value of $u^{(s^*)}$ in the $V$-independent region is approximately given by Eq.~\eqref{eq:normal-uS-half-filling}.

This effect can only be interpreted in one way, as we discuss in next section: $u^{(s^*)}$ maintains a constant value at fixed doping, as is described in Sec.~\ref{subsec:normal-uS-doping}, as long as $\tilde{t}^{(s^*)}>0$. When $V$ gets large enough,  $u^{(s^*)}$ starts decreasing because of its constraints. The net effect is the nullification of $\tilde{t}^{(s^*)}$, which is, a total localisation effect.

\subsection{Mott localisation effect}\label{subsec:normal-mott-numeric}

The most interesting result we obtained for the normal phase is the possibility of localising electrons by tuning the $V/\delta$ ratio. In Fig.~\ref{subfig:normal-uS-data-B256}, at low $V$ the convergence value of $u^{(s^*)}$ is approximately $V$-independent at sufficiently low temperatures. Let $V_\mathrm{c}[\beta,\delta]$ be the critical value such that
\[
	0 \le V < V_\mathrm{c}[\beta,\delta]
	\quad\colon\quad
	\text{$u^{(s^*)}$ is $V$ independent.}
\]
Then within the entire segment $[0,V_\mathrm{c}]$ the convergence value is (approximately) the same obtained for $V=0$. We can define in first instance the critical $V$ as the value such that the hopping correction is maximized,
\[
	V_\mathrm{c}[\beta,\delta] \equiv \frac{2t}{u^{(s^*)}[\beta,\tilde{\mu}(\delta)] \big|_{V=0}}
\]
When $V$ becomes larger than this critical value, the resulting RMP $\tilde{t}^{(s^*)}$ drops to $0$ and due to the constraint of Eq.~\eqref{eq:normal-us*-constraint} the convergence value of $u^{(s^*)}$ is forced to decrease. Note that the constraint of Eq.~\eqref{eq:normal-us*-constraint} does not prevent $u^{(s^*)}$ from saturating to its upper boundary from below. In Fig.~\ref{subfig:normal-tS-data-B128} the converged values of $\tilde{t}^{(s^*)}$, extracted from the same data of Fig.~\ref{subfig:normal-uS-data-B128}, are plotted in parametric space.

\begin{figure}
	\centering
	\subfloat[Simulated values of $\tilde{t}^{(s^*)}$]{
		\includegraphics[width=0.45\linewidth]{../Project/plots/refined/Mode=rs/Phase=Normal/Setup=B[128]/Syms=/RB=S/Obj=RMPs/xVar=δ_yVar=V/tS_t=1.0_U=0.0_L=128_β=100.0_SurfacePlot.pdf}
		\label{subfig:normal-tS-data-B128}
	}
	\subfloat[Critical Mott boundary.]{
		\includegraphics[width=0.5\linewidth]{../Project/src/labs/mott-localization/normal-mott-boundary-uS.pdf}
		\label{subfig:normal-tS-data-B128-mott}
	}
	\caption{Plot of the self-consistent convergence values for the HFP $u^{(s^*)}$ in the normal phase and the Mott-boundary $V_\mathrm{c}$, computed as is described in text. In the right panel, the tick $V=4.93$ is specified explicitly as a graphic confirm of the computation of Eq.~\eqref{eq:normal-Vc-half-filling}. For graphic reasons the points at $V_\mathrm{c} > 8$ have been omitted.}
	\label{fig:normal-tS-data}
\end{figure}

To extract the value of $V_\mathrm{c}[\beta,\delta]$ is a non-trivial analytic problem due to the insurgence of elliptic integrals and the smearing effects of temperature. Instead, for the high-$\beta$ half-filled case we can use Eq.~\eqref{eq:normal-uS-half-filling} and get:
\begin{equation}
	V_\mathrm{c}[\beta \to \infty,0] = \frac{2t}{(2/\pi)^2} \approx 4.93 t \label{eq:normal-Vc-half-filling}
\end{equation}
At half-filling, the effective bands amplitude reduction is so strong that small values of $V$ (compared to $t$) are sufficient to nullify kinetics. In Fig.~\ref{subfig:normal-tS-data-B128-mott} we report, superimposed, the convergence values of the RMP $\tilde{t}^{(s^*)}$ and $V_\mathrm{c}[\beta=100,\delta]$ computed with the simple procedure hereby described. Our prediction of the critical value for $V$ approximates reasonably the localisation region.

\begin{figure}
	\centering
	\subfloat[Temperature dependence of $u^{(s^*)}$ at various dopings.]{
		\includegraphics[width=\linewidth]{../Project/plots/refined/Mode=hs/Phase=Normal/Setup=C[256]/Syms=/RB=S/Obj=HFPs/xVar=V_pVar=β/uS_t=1.0_U=0.0_L=256_δ=Compared.pdf}
		\label{subfig:normal-data-uS-C256-compared}
	}\\
	\subfloat[Temperature dependence of $\tilde{t}^{(s^*)}$ at various dopings.]{
		\includegraphics[width=\linewidth]{../Project/plots/refined/Mode=hs/Phase=Normal/Setup=C[256]/Syms=/RB=S/Obj=RMPs/xVar=V_pVar=β/tS_t=1.0_U=0.0_L=256_δ=Compared.pdf}
		\label{subfig:normal-data-tS-C256-compared}
	}
	\caption{Comparative plots of the HFP $u^{(s^*)}$ (top panel) and RMP $\tilde{t}^{(s^*)}$ (bottom panel) in null-doped (left plots), ``medium-doped'' ($\delta=0.2$, central plots) and ``heavily-doped'' ($\delta=0.4$, right plots) contexts. Increasing temperature progressively destroys Mott localisation, reducing the bands shrinking effect.}
	\label{fig:normal-data-C256-compared}
\end{figure}

Increasing temperature Mott localisation is suppressed. Also, out precedent argument were valid only at low temperature. In Fig.~\ref{subfig:normal-data-uS-C256-compared} we report results for the HFP $u^{(s^*)}$ computed at decreasingly lower temperature and three example doping levels,
\[
	1.0 \le \beta \le 100.0
	\qquad
	\delta = 0.0, 0.2, 0.4
\]
At high temperature the $V$-independence region that was visible in Fig.~\ref{subfig:normal-uS-data-B256} completely disappears, making $u^{(s^*)}$ a monotonically descendent function. In Fig.~\ref{subfig:normal-data-uS-C256-compared} the same dataset is plotted with focus on the RMP $\tilde{t}^{(s^*)}$. Temperature increase disrupts localisation effects, at least at small values of $V$. However, the bands shrinking effect is still relevant at $\beta=1.0$, the hopping parameter approximately halving at $V \approx 5 \divisionsymbol 6$.

\subsection{Solution free energy and entropy densities}\label{subsec:normal-f-s}

In order to compare phases, we need an estimation of free energy of each. The plots of Fig.~\ref{fig:normal-f-data} report our findings on the normal phase free energy, computed based on Eq.~\eqref{eq:normal-fMFT}. The free energy is intended in unities of $t$. For a non-flat band, doping increases energy because the band fills up and electrons populate higher energy levels. 

Looking the data of Fig.~\ref{subfig:normal-f-data-B128}, at fixed $\delta$ free energy density remains pretty much constant up to the Mott boundary. This is simply because as long as a band with non-zero amplitude is present, the HFP correction of Eq.~\eqref{eq:normal-fMFT} compensates the free energy increase due to bands shrinking,
\[
\begin{aligned}
	- V \big| u^{(s^*)} \big|^2 &= - \frac{V u^{(s^*)*}}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} (\cos k_x + \cos k_y) f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu}) \\
	&= \frac{1}{L_x L_y} \sum_{\mathbf{k}\in\mathrm{BZ}} \left( \epsilon_\mathbf{k} - \tilde{\epsilon}_\mathbf{k} \right)  f(\tilde{\epsilon}_\mathbf{k};\beta,\tilde{\mu})
\end{aligned}
\]
having we used in the second passage
\[
	\tilde{\epsilon}_\mathbf{k} = \tilde{\epsilon}_\mathbf{k} + u^{(s^*)*} V (\cos k_x + \cos k_y)
\]
Recalling the first point of the derivation of Sec.~\ref{sec:normal-free-energy}, to add the HFP correction to free energy is the same as removing the \textit{tilde} sign from single-state energies, thus using the free model ($V=0$) free energy density expression.

\begin{figure}
	\centering
	\subfloat[Heatmap of $f$ in $\delta V$ plane.]{
		\includegraphics[height=0.32\linewidth]{../Project/src/labs/mott-localization/normal-mott-boundary-f.pdf}
		\label{subfig:normal-f-data-B128}
	}
	\subfloat[$V$-dependency of $f$ varying $\delta$.]{
		\includegraphics[height=0.32\linewidth]{../Project/plots/refined/Mode=hs/Phase=Normal/Setup=B[256]/Syms=/RB=S/Obj=phys/xVar=V_pVar=δ/f_t=1.0_U=0.0_L=256_β=100.0.pdf}
		\label{subfig:normal-f-data-B256}
	}
	\caption{Free energy density of the normal phase. On the left-side panel, the dashed white line has the same meaning as the dotted line of Fig.~\ref{subfig:normal-tS-data-B128-mott}, indicating the Mott transition.}
	\label{fig:normal-f-data}
\end{figure}

Mott localisation is energetically expensive. This is visible in both panels of Fig.~\ref{fig:normal-f-data}. The $V$-dependent energy increase is an entropic effect: indeed, for a flat band:
\[
	f(0;\beta,\tilde{\mu}(\delta)) = \frac 1 2
\]
at any temperature. The expression for entropy density of a free Fermi gas \cite{grosso2014solid} is given by
\[
	s(f) = -k_\mathrm{B} \left[ f \log f + (1-f) \log (1-f) \right]
\]
represented in Fig.~\ref{subfig:normal-entropy}. The maximum is located at $f=1/2$, then a flat band realises the maximally entropic condition. The Mott insulating region can be seen as a degeneration of the FS interface to the entire BZ, a process that maximises entropy.

\begin{figure}
	\centering
	\subfloat[Heatmap of $s_\mathrm{MFT}$ in $\delta V$ plane.]{
		\includegraphics[height=0.32\linewidth]{../Project/src/labs/mott-localization/normal-mott-boundary-s.pdf}
		\label{subfig:normal-s-data-B128}
	}
	\subfloat[$V$-dependency of $f$ varying $\delta$.]{
		\includegraphics[height=0.32\linewidth]{../Project/src/labs/functions/entropy.pdf}
		\label{subfig:normal-entropy}
	}
	\caption{Entropy density of the normal phase (left panel) and theoretical contribution to entropy for a state with occupation $f$. On the left-side panel, the dashed white line has the same meaning as the dotted line of Fig.~\ref{subfig:normal-tS-data-B128-mott}, indicating the Mott transition.}
	\label{fig:normal-s-data}
\end{figure}

All of this holds, as all the rest, at low temperature: by lowering $\beta$, finite-temperature effects become more and more relevant and the statements above need to be corrected.

\subsection{Convergence problems and mixing fine-tuning}\label{subsec:normal-g-optimization}

The normal phase is simple enough to describe some of the typical convergence problems arising in HF iterative schemes anticipated in Sec.~\ref{subsec:reject-mix-iterate}. Bands flattening is a source of algorithmic instability: numerics can lead to values of $u^{(s^*)}$, during iterations, slightly out of bounds with respect to Eq.~\eqref{eq:normal-us*-constraint}. Even if the numeric effect is small, the consequence can be vast. To heal this effect and extract the actual convergence values, it is necessary to optimise the mixing parameter $g \in [0,1]$. 

\begin{figure}[t]
	\centering
	\includegraphics[width=\linewidth]{../Project/src/labs/tracks/tracks.pdf}
	\caption{Comparison of three example tracks for the single-valued HFPs vector of the normal phase, simulated at a ``Mott-deep'' point.}
	\label{fig:normal-tracks-compared}
\end{figure}

We defined $g$ in Sec.~\ref{subsec:reject-mix-iterate}, and operatively in Eq.~\eqref{eq:mix}. This parameter controls deterministically how, from an initialiser (the HFPs vector we plug in the algorithm) and an output (the HFPs vector we get back from the algorithm) the new initialiser for the following step is generated. We show in Fig.~\ref{fig:normal-tracks-compared} the evolutions at different mixing parameters of a simulation at
\[
	V = 6.0
	\qquad
	\delta = 0.1
	\qquad
	\beta = 100.0
\]
This point is inside the Mott region, as can be checked in Fig.~\ref{subfig:normal-uS-data-B128}. The HFP $u^{(s^*)}$ needs to converge at $2t/V=t/3$ in order to give complete localisation. The initialiser
\[
	\text{Step $1$}
	\quad\colon\quad
	u^{(s^*)} = 0.1
\]
was arbitrarily chosen. In each panel of Fig.~\ref{fig:normal-tracks-compared}, the red track represents the value of $u^{(s^*)}$ we plug in the self-consistency equations, the blue the result of the self-consistent equation, the green one the mixed value of the two based on different $g$s. The gray dashed line is the theoretical convergence target. 

In the first two panels, there is an evident infinite loop occurring whenever the green line crosses the threshold $2t/V$. At those steps, the green point is out of bounds with respect to Eq.~\eqref{eq:normal-us*-constraint}. When this happens, the following red point gives a band with inverted sign, a feature that ``breaks'' the self-consistency equation and generates a blue point with no physical significance.

To overcome this problem it is necessary to move ``slowly''. As is shown in the right panel, the choice of a small enough mixing parameter ensures convergence. In the track, each blue dot is above the target and would produce a band with inverted sign. Mixing blue and red dots with a $95\%$ unbalance on the red ones, we ensure that the green track approaches the target from below. Note, however, that lowering $g$ cannot be reiterated too many times. Indeed, for $g \to 0$, any point of our map becomes a fixed point. 

This kind of problems can turn out to be pretty common for iHF schemes, and to understand how to knob the algorithmic parameters without sacrificing accurateness and precision, as well as avoiding computationally costly and frustrating infinite loops, was an essential part of this algorithm design.